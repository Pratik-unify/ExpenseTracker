{
  "name": "expense-tracker-root",
  "version": "1.0.0",
  "description": "Root runner for Expense Tracker application",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client/frontend",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client/frontend",
    "dev": "npx concurrently \"npm run server\" \"npm run client\""
  }
}
